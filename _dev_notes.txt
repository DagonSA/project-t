Tile Coordinate System - Offset odd-r as a tilemap source of truth, axial conversions for math only


TO DO RIGHT NOW:
	
	IN PROGRESS:


Oh interesting — so something is typed against Character 
somewhere and breaks without it. 
Worth a quick hunt to find what's actually depending on it, 
because that'll tell you if the class name is load-bearing for type-checking, 
or if something is doing is Character or using it as a type hint.

(No VBex help) - creating EventTokenManager and we';ll see


"static classes / pure data classes in GDScript — when to use static vars and funcs, 
RefCounted vs Node vs no extends, and how that affects instantiation"

	
UI updates + 
AFTER MOVEMENT ACTIONS! - need to find a token image
(scout, token, passive, cards)
We could do -> if event token, uncover token (stuff) 
	then - give player a choice (passive, play card, basic attack) 

	We need to have a "movement finished" signal, or sth similar. 
	
	Update UI text for current teams when movement
	Hide/show "end turn" when movements are finished (or end movement)
	Indicate movement actions with small icon
	Movement animation
	Maybe - for selecting 'enemy' character the selection should be red? 

	We could also indicate move actions possibility with small icon or sth 
	
			2) Movement -> Create states (like idle, moving, etc) for flag check for input handler. When clicked tile, if state (from game mode) is == X, then go to the next movement phase (draw arrow)
			3) Standing positions + actual movement
			4) Create a diagram of what is connected to what and how (ex. movement gets signal from game mode.. etc) 
			5) Learn about bulletproofing and potentially testing. All these (board manager != null etc)

	FOR LATER:
		Formations with multiple teams, etc. 
		Formations on the origin tile after having more than 1 movement phase 

	BIG POLISH:
		1) Eventually have highlighters without borders on a field where there is range:
				Only 'external border'. not internal one - to see availability better
		2) Movement arrow - show the point, color, width, etc. Maybe even some curve
		
	BUGS/ISSUES:
		1) Sometimes there's a case when clicking a tile with freshly selected character doesn't register
		2) Check if we should split character data into base (ex. Actions: always 1) and character based (etc Strength)
		
	
	
	
	
	
	
	
	
	To REFACTOR: 
		1) Potential READY phase before Turn 1
		2) Movement phase - alternate between players
		2) Signals sending payloads instead of arguments (to bulletproof stuff)
		3) Consider lookint at all tile-related code/spawners etc and think if we can change everything 
		to godot grid-based stuff (instead of using axial math etc)
		4) Move hex math to a separate helper? 
		5) Right Now Player Spawner is asking Tilemap for positions. Refactor. 
		
---------------------------------------------
